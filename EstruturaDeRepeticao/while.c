#include <stdio.h>

int main(){

/*while (condicao){
    // código
}
*/
    int i = 1;

    while (i <= 5) { // condição
        printf("%d\n", i); // imprimir o valor na tela
        i++; // incremento (++ pega o valor atual de i e incrmenta +1)
    }


    // A condição é booleana (verdadeiro ou falso)
    // A partir do momento que a condição se torna falsa ele para de executar

}